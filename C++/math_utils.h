#ifndef MATH_UTILS
#define MATH_UTILS

struct Rectangle {
    double length;
    double width;
};

double area(int length, int width);

double area(int length);

double area(Rectangle retangle);

#endif